import Field from '@interfaces/field';

const translateEnglishCommandField = {
	title: '!translate-english',
	description: '**`Command which translates the given text to english.`**',
} as Field;

const translateGermanCommandField = {
	title: '!translate-german',
	description: '**`Command which translates the given text to german.`**',
} as Field;

const translateFrenchCommandField = {
	title: '!translate-french',
	description: '**`Command which translates the given text to french.`**',
} as Field;

export { translateEnglishCommandField, translateGermanCommandField, translateFrenchCommandField };
